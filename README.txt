Type $ make to comile and $ make run to run the program.

The results will be stored in a file called output.txt.

The makefile is in a compressed folder because I can't upload it. Please take it out of the folder and run it in the same directory as the rest of the files.